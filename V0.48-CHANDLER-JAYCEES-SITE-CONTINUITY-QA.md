# Arizona Connect v0.48 — Chandler Jaycees Site Continuity QA

Status: **RC1 — structural/data QA PASS; browser/visual gate externally unavailable pending normal runtime**

## Working-tree QA

**11,377 / 11,377 PASS**

Key gates:
- 172 contiguous public claims with exact claim-page/index parity.
- 115 governed source routes with exact source crosswalks.
- 42 track records: 11 current / 30 historical / 1 corroboration-open lead.
- 85 Timeline milestones + 4 open questions; AZ-TL-Q001 retired and AZ-TL-0086 admitted.
- 18 mapped records / 24 intentionally unpinned.
- AZ-TRK-0102 is the sole authorized Geographic Absolute location-object change.
- AZ-TRK-0102 promoted from CITY_ONLY_NO_POINT to SITE_POINT_APPROXIMATE at 33.299440, -111.825837.
- AZ-TRK-0001 current Chandler location object remains byte-for-byte unchanged.
- 41/42 inherited v0.47 location objects remain exactly unchanged.
- Arizona authoritative SVG and mask remain byte-for-byte unchanged.
- Scottsdale Jaycees Rodeo BMX Park remains unpinned.
- Events, relationships and media registers remain protected and unchanged.
- Root launcher remains byte-for-byte unchanged.
- All JSON parses; all JavaScript passes `node --check`.
- HTML ID, image-alt, noopener and local-reference gates pass.
- Public stale-state firewall passes for v0.47 claim/source/map/timeline counts and the retired Chandler lineage language.
- Expected-change firewall reports zero unauthorized inherited-file changes and zero inherited-file removals.

## Evidence boundary

v0.48 supports the historical Chandler Jaycees BMX record and the current Chandler BMX record occupying the same physical Frye/McQueen track site. It does **not** claim the current exact coordinate is the exact 1975 or 1979–1983 dirt-course center, that course geometry stayed identical, or that every operator/sanction/legal transition has been reconstructed.

## Clean-package gate

A clean extraction of the RC tree independently reproduced **11,377 / 11,377 PASS** and **HTTP 200** over loopback. Package checksum verification found **470/470 governed payload hashes correct**, with zero checksum failures, zero unlisted payloads, zero missing payloads and zero unsafe ZIP paths.

The managed browser gate remains externally unavailable: Chromium refuses root execution unless its sandbox is disabled. `--no-sandbox` was not used.
