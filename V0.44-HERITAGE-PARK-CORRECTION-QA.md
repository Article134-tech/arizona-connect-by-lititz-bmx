# Arizona Connect v0.44 — Heritage Park Geographic Correction 1 QA

**Result: 352/352 STRUCTURAL / DATA PASS**  
**Browser runtime / visual gate: OPEN — environment cannot initialize Chromium without disabling its sandbox**  
**Release disposition: RC1 — NOT SEALED**

## Correction verified

- `AZ-TRK-0128 — Heritage Park BMX` is retained as a historical identity but corrected from **Prescott** to **Phoenix**.
- The May 1985 BMX Action Arizona directory is governed as `Phoenix, Heritage Park BMX` with **Kathy Scott** and published numbers `602-861-6070` / `602-578-5620`.
- The recovered June 1985 BMX Plus! Arizona directory does **not** repeat Heritage Park BMX; it lists **Mesa BMX** with Kathy Zosky and Debbie Cotton instead.
- The earlier Prescott municipal-park synthesis is rejected as BMX-site evidence.
- The former Prescott `SITE_POINT_APPROXIMATE` at `34.6133572, -112.4404465` is withdrawn.
- Heritage Park BMX is now **CITY_ONLY_NO_POINT**. No Phoenix replacement coordinate is admitted.

## Governed state

- Public claims: **167**
- Governed source IDs/routes: **112**
- Track census: **40 — 11 current / 28 historical / 1 corroboration-open lead**
- Timeline: **80 published milestones + 5 questions**
- Events: **28**
- Relationships: **15**
- Media routes: **19**
- Geographic Absolute: **17 mapped / 23 intentionally unpinned**

## Geographic Absolute gate

- Exactly **39/40** v0.43 location objects are byte-for-byte/data-object identical.
- `AZ-TRK-0128` is the **sole** changed location object.
- The 17 remaining mapped records are an exact unchanged subset of the previously verified v0.43 mapped set.
- The sole removed coordinate is `AZ-TRK-0128 — 34.6133572, -112.4404465`.
- The authoritative Arizona SVG boundary and geographic mask remain byte-for-byte unchanged.
- Atlas renders **17** pins and the accessible point list contains **17** links; Heritage Park is absent from both.

## Integration / source-correction gates

- `AZ-PUB-0136`, `AZ-PUB-0137`, and `AZ-PUB-0138` are the v0.44 correction cohort.
- Current Timeline removes `AZ-TL-0072` and corrects `AZ-TL-0073` to the May 1985 Phoenix Heritage Park occurrence.
- Recovery embedded data and Timeline JavaScript both carry the corrected Phoenix / city-only Heritage state.
- No active Atlas/Research runtime data retains the old Prescott Heritage status or old repeated-directory narrative.
- Historical v0.35 control and claim snapshots remain unchanged as audit history.
- Events, relationships, and media registers are byte-for-byte inherited.
- Locked launcher and authoritative Arizona geography assets are unchanged.

## Structural gates

The automated suite verifies, among other controls:

- all **167** claim pages present;
- all **74 JSON** files parse;
- all **9 JavaScript** files pass syntax checks;
- **220 HTML pages** checked;
- zero broken local references;
- zero duplicate HTML IDs;
- zero images missing `alt`;
- zero unsafe `target="_blank"` links;
- zero unexpected inherited-file changes;
- zero inherited files removed;
- only the expected v0.44 correction files added before this QA record.

## Runtime gate

The candidate serves successfully on `127.0.0.1` and returns **HTTP 200** to a normal HTTP client. The installed Chromium process refuses root execution unless launched with `--no-sandbox`. Disabling the browser sandbox would weaken a security control, so it was not done. Arizona Connect therefore never reached browser DOM rendering in this environment. This is an external runtime limitation, not a demonstrated product failure.

## Evidence boundary

v0.44 does **not** claim an exact Phoenix Heritage Park BMX site, operator/ownership role, opening/closing span, sanction continuity, or relationship to Desert Edge BMX, Roadrunner Raceway, or another Phoenix-area track. Mile-Hi remains parked at `CITY_ONLY_NO_POINT` pending direct period subscriber/address evidence.
